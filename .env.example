# n8n Environment Variables Example
# Copy this file to .env and adjust the values as needed.

# --- n8n Core Configuration ---
N8N_PORT=5678
N8N_HOST=localhost
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/
GENERIC_TIMEZONE=UTC

# Enforce correct permissions for n8n settings file (recommended)
# Set to true to automatically enforce 0600 permissions, or false to disable the check.
N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true

# --- n8n Basic Authentication (Recommended for production) ---
# Set N8N_BASIC_AUTH_ACTIVE to true to enable basic auth.
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=secure-password-for-n8n  # IMPORTANT: Change this to a strong, unique password!

# --- PostgreSQL Database Configuration ---
# These settings connect n8n to its PostgreSQL database.
# If you change POSTGRES_USER or POSTGRES_DB here, also update the healthcheck in docker-compose.yml
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgres
DB_POSTGRESDB_PORT=5432
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=secure-password-for-postgres # IMPORTANT: Change this to a strong, unique password!